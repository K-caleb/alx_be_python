#rectangle_area.py

# Define the variables for the rectangle's dimensions
length = 10  # Represents the length of the rectangle
width = 5    # Represents the width of the rectangle

# Calculate the area of the rectangle using the formula: Area = length * width
area = length * width

# Print the calculated area in the specified format
print(f"The area of the rectangle is: {area}")